#include <iostream>
using namespace std;

int main() {
    // Infinite for loop
    for (;;) {
        cout << "This will run forever!" << endl;
    }
    return 0;  // This line is never reached
}
